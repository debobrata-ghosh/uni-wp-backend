name: Deploy themes + plugins to AWS

on:
  push:
    branches: ["main"]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: deploy-main
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy via rsync over SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.AWS_HOST }}
          username: ${{ secrets.AWS_USER }}
          key: ${{ secrets.AWS_SSH_KEY }}
          port: ${{ secrets.AWS_PORT }}
          script: |
            set -e
            mkdir -p "${{ secrets.AWS_WP_PATH }}/wp-content/themes" "${{ secrets.AWS_WP_PATH }}/wp-content/plugins"

      - name: Rsync themes
        uses: burnett01/rsync-deployments@7.0.1
        with:
          switches: -avzr --delete
          path: wp-content/themes/
          remote_path: ${{ secrets.AWS_WP_PATH }}/wp-content/themes/
          remote_host: ${{ secrets.AWS_HOST }}
          remote_user: ${{ secrets.AWS_USER }}
          remote_key: ${{ secrets.AWS_SSH_KEY }}
          remote_port: ${{ secrets.AWS_PORT }}

      - name: Rsync plugins
        uses: burnett01/rsync-deployments@7.0.1
        with:
          switches: -avzr --delete
          path: wp-content/plugins/
          remote_path: ${{ secrets.AWS_WP_PATH }}/wp-content/plugins/
          remote_host: ${{ secrets.AWS_HOST }}
          remote_user: ${{ secrets.AWS_USER }}
          remote_key: ${{ secrets.AWS_SSH_KEY }}
          remote_port: ${{ secrets.AWS_PORT }}

